name: Corp – Terraform Apply

on:
  workflow_dispatch: {}

  push:
    branches:
      - main
    paths:
      - "env/landingzones/corp/**"
      - "modules/**"
      - ".github/workflows/corp-apply.yml"

  workflow_run:
    workflows: ["Platform – Terraform Apply"]
    types:
      - completed

permissions:
  id-token: write
  contents: read

jobs:
  terraform_plan:
    name: Terraform Plan (Corp)
    runs-on: ubuntu-latest
    environment: corp
    if: |
      (github.event_name == 'push') ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')

    defaults:
      run:
        shell: bash
        working-directory: env/landingzones/corp

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_CORP }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: corp-tfplan
          path: env/landingzones/corp/tfplan

  terraform_apply:
    name: Terraform Apply (Corp)
    runs-on: ubuntu-latest
    environment: corp
    needs: terraform_plan
    if: needs.terraform_plan.result == 'success'

    defaults:
      run:
        shell: bash
        working-directory: env/landingzones/corp

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_CORP }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: corp-tfplan
          path: env/landingzones/corp

      - name: Terraform Apply (from plan)
        run: terraform apply -auto-approve tfplan
